# 基于 Spring + MySQL 的汽车租赁系统（毕业设计）开发说明

## 1. 项目目标

开发一个面向校内毕业设计的汽车租赁管理系统，支持用户在线租车、订单管理、车辆管理、费用结算与后台运营统计。

系统分为两端：
- 用户端（Web）：浏览车辆、下单租车、支付订单、查看历史记录。
- 管理端（Web）：车辆管理、订单审核、还车验收、费用结算、数据统计。

---

## 2. 技术选型

### 2.1 后端
- `Java 17`
- `Spring Boot 3.x`
- `Spring MVC`
- `Spring Security + JWT`（登录鉴权）
- `MyBatis-Plus`（ORM）
- `MySQL 8.x`
- `Maven`

### 2.2 前端
- `Vue 3 + Vite`
- `TypeScript`
- `Pinia`
- `Vue Router`
- `Element Plus`
- `Axios`

---

## 3. 角色与权限

- 普通用户
  - 注册/登录
  - 浏览车辆、筛选车辆
  - 提交租车订单
  - 取消订单、支付订单
  - 查看个人订单与租赁记录

- 管理员
  - 用户管理（禁用/启用）
  - 车辆管理（新增/编辑/上下架）
  - 订单管理（审核、出车、还车、结算）
  - 报表统计（营业额、订单量、车辆利用率）

---

## 4. 核心业务流程

1. 用户注册并登录。
2. 用户选择车辆与租赁时间，提交订单。
3. 系统校验车辆时间段是否可租。
4. 用户支付押金/租金，订单状态变更为“待取车/进行中”。
5. 管理员线下交车后确认“已出车”。
6. 用户还车，管理员录入里程/油量/违章/损坏信息。
7. 系统自动计算最终费用并结算。
8. 订单完成，进入历史记录。

---

## 5. 系统架构（建议）

```text
前端(Vue3) -> REST API(Spring Boot) -> MySQL
```

后端采用分层架构：
- `controller`：接口层
- `service`：业务层
- `mapper`：数据访问层
- `entity/dto/vo`：对象模型
- `config/security/common`：公共与配置模块

---

## 6. 数据库设计（核心表）

## 6.1 表清单
- `sys_user` 用户表
- `sys_role` 角色表
- `sys_user_role` 用户角色关联表
- `car_info` 车辆信息表
- `car_image` 车辆图片表
- `rental_order` 租赁订单表
- `order_payment` 支付记录表
- `pickup_return_record` 取还车记录表
- `sys_notice` 公告表（可选）
- `sys_operation_log` 操作日志表（可选）

## 6.2 关键字段设计（示例）

### `sys_user`
- `id` bigint PK
- `username` varchar(50) 唯一
- `password` varchar(100) 加密后密码
- `phone` varchar(20)
- `email` varchar(100)
- `real_name` varchar(50)
- `id_card` varchar(30)
- `status` tinyint（1正常 0禁用）
- `created_at` datetime
- `updated_at` datetime

### `car_info`
- `id` bigint PK
- `brand` varchar(30)
- `series` varchar(30)
- `model` varchar(50)
- `plate_no` varchar(20) 唯一
- `seat_count` int
- `gearbox` varchar(20)（自动/手动）
- `fuel_type` varchar(20)
- `day_rent` decimal(10,2)
- `deposit` decimal(10,2)
- `status` tinyint（1可租 2维护 3下架）
- `mileage` int
- `cover_url` varchar(255)
- `created_at` datetime
- `updated_at` datetime

### `rental_order`
- `id` bigint PK
- `order_no` varchar(40) 唯一
- `user_id` bigint
- `car_id` bigint
- `rent_start_time` datetime
- `rent_end_time` datetime
- `rent_days` int
- `day_rent` decimal(10,2)
- `deposit` decimal(10,2)
- `base_amount` decimal(10,2)
- `extra_amount` decimal(10,2)
- `total_amount` decimal(10,2)
- `order_status` tinyint
  - `10`待支付
  - `20`待取车
  - `30`租赁中
  - `40`待结算
  - `50`已完成
  - `90`已取消
- `remark` varchar(255)
- `created_at` datetime
- `updated_at` datetime

---

## 7. 接口设计（V1）

统一前缀：`/api/v1`

### 7.1 认证接口
- `POST /auth/register` 用户注册
- `POST /auth/login` 登录
- `GET /auth/profile` 当前用户信息

### 7.2 车辆接口
- `GET /cars` 分页查询车辆（支持品牌、价格区间、状态）
- `GET /cars/{id}` 车辆详情
- `POST /admin/cars` 新增车辆（管理员）
- `PUT /admin/cars/{id}` 编辑车辆（管理员）
- `PUT /admin/cars/{id}/status` 上下架/维护（管理员）

### 7.3 订单接口
- `POST /orders` 创建订单
- `GET /orders/my` 我的订单
- `GET /orders/{id}` 订单详情
- `PUT /orders/{id}/cancel` 取消订单
- `POST /orders/{id}/pay` 支付订单
- `GET /admin/orders` 后台订单分页
- `PUT /admin/orders/{id}/pickup` 确认出车
- `PUT /admin/orders/{id}/return` 确认还车并结算

---

## 8. 前端页面规划

### 8.1 用户端
- 登录/注册页
- 首页（车辆推荐）
- 车辆列表页
- 车辆详情页
- 下单确认页
- 我的订单页
- 订单详情页
- 个人中心页

### 8.2 管理端
- 后台登录页
- 控制台（统计面板）
- 车辆管理页
- 订单管理页
- 用户管理页
- 公告管理页（可选）

---

## 9. 开发里程碑（建议 6 周）

- 第 1 周：需求分析、ER 图、数据库设计、接口文档初版
- 第 2 周：后端基础工程搭建（鉴权、统一返回、异常处理）
- 第 3 周：车辆模块 + 用户模块后端完成
- 第 4 周：订单模块 + 支付/结算逻辑完成
- 第 5 周：前端用户端 + 管理端联调
- 第 6 周：测试、性能优化、文档与答辩材料整理

---

## 10. 非功能需求

- 安全性：密码加密存储、JWT 鉴权、接口权限控制
- 可用性：关键操作日志记录、统一异常提示
- 可维护性：分层清晰、模块化开发、接口版本化
- 可扩展性：后续可扩展短信通知、优惠券、企业租车

---

## 11. 交付物清单（毕业设计）

- 源代码（前端 + 后端）
- 数据库脚本（建表 + 初始化数据）
- 接口文档（Swagger/OpenAPI）
- 系统设计说明书（本文件可作为基础）
- 测试报告
- 部署说明（本地/云服务器）
- 答辩演示 PPT

---

## 12. 下一步

下一阶段建议直接开始工程初始化：
1. 初始化后端 Spring Boot 项目骨架。
2. 初始化前端 Vue3 项目骨架（用户端与管理端可同仓多应用）。
3. 输出第一版数据库 SQL 与接口实体代码。

